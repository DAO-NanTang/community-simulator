# 当前版本修复方案

> 2026-07-09 | 基线：7627 行（四阶段修复完成后）
> 审查结论：5 项中 4 项已实现，仅 2 处代码修复 + 1 处方案更新

---

## 审查结果对照

| 编号 | 项目 | 状态 | 实际代码位置 |
|:--:|------|:--:|------|
| P0-1 | 任务排序 | ✅ 已实现 | workspace: L2697-2698；questboard: L3017-3018（`statusOrder`） |
| P0-2 | 勋章展示 | ✅ 已实现 | L2726-2730，`ROLE_BADGE_ICONS` + `getUserBadges` |
| P0-3 | 标签精简 | ✅ 已修复 | Row 1 已含 scope + quick + 自定义标签；Row 2 已移除；L3001 简化为通用匹配 |
| P1-1 | 审核人下拉 | ✅ 已实现 | CSS L156-165，`showReviewerPicker`/`selectReviewer` L3279-3329，无 sidebar/backdrop |
| P2-1 | Workspace 布局 | ✅ 已实现 | L2715-2741，flex + 头像始终显示 + flow/settings/tasks 三分支 |

---

## 本次执行（2 项修复）

### 修复 1：删除 claimed 重复排序

**问题**：`claimed` 被 sort 了两次（旧 L3017 + L3019-3020），第一次是简化版（进行中优先），第二次是完整 statusOrder。保留后者，删除前者。

**位置**：`renderQuestBoard`，`urgentC.sort(qSortDL)` 之后

### 修复 2：questLocFilter 硬编码清理

**问题**：`questLocFilter` 中有三个硬编码值（在地任务 / 非在地任务 / 日常任务），与自定义标签体系冲突。简化为通用匹配。

**位置**：`renderQuestBoard` 过滤逻辑中 L3001-3004

**旧逻辑**（4 行）：
```js
if (questLocFilter==='在地任务' && t.location!=='local') return false;
if (questLocFilter==='非在地任务' && t.location==='local') return false;
if (questLocFilter==='日常任务' && t.type!=='日常') return false;
if (questLocFilter && questLocFilter!=='在地任务'&&... && t.type!==questLocFilter) return false;
```

**新逻辑**（1 行）：
```js
if (questLocFilter && t.type !== questLocFilter) return false;
```

> 如需「在地任务」筛选，管理员可创建同名自定义标签（已有标签输入框，L2974）。

---

## 不做

- ❌ P0-1/P0-2/P1-1/P2-1 — 均为已实现项，无需重复执行
- ❌ questFilterBar HTML 保留 — 它提供类型过滤（全部/主线/支线/共创/隐藏），与 scope 过滤不重复
