# 身份系统统一方案

> 2026-07-10 | 基于当前 dashboard.html 现状
> 目标：所有角色变更走同一个函数，存储同步、界面刷新、历史记录一步到位

---

## 〇、现状问题

角色修改散落在 7 处，每处只做了部分工作：

| # | 位置 | 触发场景 | 更新 users 表 | 更新 data.members | 更新 currentUser | 刷新界面 | 记 history |
|---|------|---------|:---:|:---:|:---:|:---:|:---:|
| ① | `registerUser()` L1967 | 注册 | ✅ | ✅ | ❌ | ❌ | ❌ |
| ② | `upgradeToNPC()` L2308 | 邀请码升NPC | ✅ | ❌ | ✅ | ❌ | ❌ |
| ③ | `doUpgradeWithCode()` L2532 | 邀请码升冒险者 | ✅ | ❌ | ✅ | ✅(enterWorld) | ❌ |
| ④ | `doUpgradeWithCode()` L5471 | 世界码/NBC码 | ✅ | ❌ | ✅ | ✅(enterWorld) | ❌ |
| ⑤ | 成员档案表格 L7431 | 管理员手动改 | ❌ | ✅ | ❌ | ❌ | ❌ |
| ⑥ | Step3 分配卡片 L8553 | 创世终端 | ❌ | ✅ | ❌ | ❌ | ❌ |
| ⑦ | Step3 改卡片角色 L8653 | 创世终端 | ❌ | ✅(staff_cards) | ❌ | ❌ | ❌ |

**后果**：部分场景角色改了但界面不刷新；部分场景刷新了但 users 表没同步导致下次登录角色回退；全部场景都没有写 `season_history`。

---

## 一、新增 `changeUserRole()` 统一函数

### 函数签名

```javascript
function changeUserRole(name, newRole, opts) {
  // name:    用户名
  // newRole: 'admin' | 'builder' | 'adventurer' | 'npc' | 'visitor'
  // opts:    { period, inviteCode, fromRole, staffCard }
  //           period:      '第四期'（可选，写进 season_history）
  //           inviteCode:  'NT-A3F2'（可选，升级路径使用的邀请码，自动消耗）
  //           fromRole:    旧角色（可选，不传则自动从当前数据读取）
  //           staffCard:   { role: 'NT', ... }（可选，共建者卡片信息）
}
```

### 函数内部做的事（5 步）

```
changeUserRole('小明', 'adventurer', { period: '第四期', inviteCode: 'NT-A3F2' })
    │
    ├─ Step 1: 读旧角色，写 season_history
    │   从 users 表读当前角色 → 追加到 users[name].season_history[]
    │   记录格式：{ role: 'visitor', period: '第四期', changed_at: '2026-07-10' }
    │
    ├─ Step 2: 更新 users 表（浏览器 localStorage）
    │   users[name].role = newRole
    │   如果有 world_id → 写入
    │   如果有 staffCard → 写入关联
    │
    ├─ Step 3: 更新 data.members（营地数据）
    │   if (data.members[name]) data.members[name].role = newRole
    │
    ├─ Step 4: 如果改的是当前登录用户
    │   currentUser.role = newRole
    │   refreshUserHUD()
    │   showMainInterface(newRole, 'workspace')  ← 界面立刻刷新
    │
    └─ Step 5: 消耗邀请码（如果传了 inviteCode）
        从 world_codes / invite_codes 池中删除该码
        保存 data 到 localStorage
```

### 关键约束

- **管理员不可被降级**：如果 `name === '砚仁'`（预设 admin），拒绝改为非 admin
- **共建者必须有冒险者历史**：改为 builder 前检查用户曾为 adventurer
- **一次只改一个角色**：不会出现同时触发两条路径的冲突

---

## 二、5 条身份升级路径

### 路径 A：注册 → 云村民

```
注册页填名字+密码+头像 → doRegister()
    → registerUser() 创建用户（role='visitor'）
    → loginUser() → enterWorld()
    → 无 lastPage → showPlazaPlaceholder() → 村口
```

- 不调 `changeUserRole`（初始创建，不是变更）
- 注册时默认就是 visitor

### 路径 B：云村民 → 冒险者

```
村口 → 活动信息站 → 选营期 → 输入冒险者邀请码
    → changeUserRole(name, 'adventurer', {
          period: '第四期',
          inviteCode: 'NT-A3F2',
          fromRole: 'visitor'
      })
    → 界面刷新：标签栏出现 📋任务大厅
    → 邀请码 NT-A3F2 从池子删除
    → season_history 追加 { role: 'adventurer', period: '第四期' }
```

**调用位置**：`doUpgradeWithCode()` 中的两处（L2532, L5471）合并为一处调用

### 路径 C：云村民 → 在地 NPC

```
触发方式一：村口「实景地图」→ 输入 NPC 邀请码
触发方式二：注册满 7 天 → 系统提示 → 确认升级
    → changeUserRole(name, 'npc', {
          inviteCode: 'NPC-X1Y2',  // 方式一有码，方式二无码
          fromRole: 'visitor'
      })
    → 界面不变（NPC 和云村民标签栏相同，但可以领在地任务了）
    → world_id 写入
```

**调用位置**：`upgradeToNPC()` L2308 改为调用统一函数

### 路径 D：冒险者 → 共建者

```
创世终端 Step3 → 管理员分配角色卡片给冒险者
    → changeUserRole(name, 'builder', {
          fromRole: 'adventurer',
          staffCard: { role: 'NT', group: '课程' }
      })
    → 界面刷新：标签栏变成共建者版
    → staff_cards 写入选中的卡片
```

**调用位置**：Step3 分配逻辑 L8553、L8653 合并

### 路径 E：冒险者 → NPC（营期结束）

```
管理员点「完结本期」→ 扫描所有 period 匹配的 adventurer
    → 批量 changeUserRole(name, 'npc', {
          fromRole: 'adventurer',
          period: '第四期（完结）'
      })
    → season_history 追加，不丢勋章
    → 下次登录：营地任务显示「🔒 上期已完结」
```

**调用位置**：`closePeriod()`（暂未实现，Phase 10/11 时做）

---

## 三、7 处调用点改造

| # | 原来 | 改为 |
|---|------|------|
| ① | `registerUser()` 直接写 role | 不变（初始创建） |
| ② | `upgradeToNPC()` 手动写 3 处 | `changeUserRole(name, 'npc', { inviteCode, fromRole })` |
| ③ | `doUpgradeWithCode()` L2532 | 合并到 ④ |
| ④ | `doUpgradeWithCode()` L5471 | `changeUserRole(name, 'adventurer', { period, inviteCode, fromRole })` |
| ⑤ | 成员档案表格直接改 `data.members` | `changeUserRole(name, newRole, { fromRole })` |
| ⑥ | Step3 分配卡片 L8553 | `changeUserRole(name, 'builder', { fromRole: 'adventurer', staffCard })` |
| ⑦ | Step3 改卡片角色 L8653 | 合并到 ⑥ |

---

## 四、season_history 格式

```javascript
// users['小明'].season_history
[
  { role: 'visitor',   period: '',        changed_at: '2026-06-01' },
  { role: 'adventurer', period: '第四期',  changed_at: '2026-07-01' },
  { role: 'npc',        period: '第四期（完结）', changed_at: '2026-07-20' },
]
```

### 用途

- **个人页**：显示"曾参与：第四期冒险者、第五期冒险者"
- **勋章**：`getUserBadges()` 直接读这个数组，升级不丢
- **活跃度**：算参与了几期

---

## 五、和已有基础设施的关系

本次方案依赖已完成的：

| 基础设施 | 状态 | 用途 |
|---------|:---:|------|
| `ROLE_CAPABILITIES` | ✅ 已建 | 角色能力表 |
| `userCan(user, cap)` | ✅ 已建 | 权限查询 |
| `userTabs(user)` | ✅ 已建 | 标签栏查询 |
| `showMainInterface(role, tab)` | ✅ 已建 | 界面刷新统一入口 |
| `saveLastPage()` / `getLastPage()` | ✅ 已建 | 位置记忆 |

---

## 六、实现步骤

### Step 1：写 `changeUserRole()` 函数（~40 行）

插入位置：`ROLE_CAPABILITIES` 定义下方（L1888 附近）

### Step 2：改造 6 处调用点（每处 ~3 行变 1 行）

- `upgradeToNPC()` L2308
- `doUpgradeWithCode()` L2532 + L5471 合并
- 成员档案表格 L7431
- Step3 分配卡片 L8553 + L8653 合并

### Step 3：验证

- 注册 → 村口 ✅
- 邀请码升级 → 界面刷新 ✅
- 管理员手动改 → 同步生效 ✅
- 退出再登录 → 角色不丢 ✅
- season_history 有记录 ✅

### 总改动量：~60 行新增，~20 行删除

---

## 七、不在此方案的内容

- `closePeriod()`（营期完结批量转换）→ Phase 10/11
- 自动检测 7 天升级 NPC → Phase 0 村口改版时做
- 管理员不可降级的前端校验 → Step 2 时加