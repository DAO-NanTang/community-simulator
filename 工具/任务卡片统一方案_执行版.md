# 任务卡片统一方案 · 执行版

> 2026-07-08 | 目标 AI 可无上下文执行
> 每个 Step 含：行号 + 旧代码 → 新代码 + 验证方法
> 执行基线：dashboard.html（当前 commit: 98e7a5c）
> 审查状态：✅ 已通过两轮审查，阻塞问题已修复

---

## 〇、前置：基线确认

执行前确认 `dashboard.html` 处于以下状态：

| 函数 | 行号 | 说明 |
|------|------|------|
| `renderQuestCard` | 2729-2808 | 共用卡片函数，已支持 village/questboard/workspace |
| `renderVillageQuestHall` | 1891-2026 | 村口任务大厅，已调用 renderQuestCard |
| `renderQuestBoard` | 2811-2917 | 主界面任务大厅，已调用 renderQuestCard |
| `renderWorkspace` | 2521-2694 | 砚仁工作台，❌ 自绘卡片 |
| `renderMemberDetailFor` | 3923-3986 | 成员档案，❌ 自绘卡片 |
| `renderDailyView` | 3373-3536 | 工会大厅，❌ 自绘卡片 |

---

## Step 1：扩展 renderQuestCard（行 2729-2808）

### 改动 1.1：展开回调 — 散三元 → 映射表（修复阻塞问题 4.1）

**位置**：第 2755 行

**旧代码**（约在第 2755 行，在 `h += '<div style="display:flex;...cursor:pointer" onclick="...` 的末尾）：
```js
'+(ctx==='village'?'renderVillageQuestHall()':ctx==='questboard'?'renderQuestBoard()':'renderWorkspace()')+'
```

**新代码**：在第 2733 行（`var tasks = data.tasks || []; var idx = ...`）之后、`var ctx = ...` 之后，插入映射表。然后将 onclick 尾部的散三元替换：

在函数开头变量声明区域（约第 2733 行之后，`var h` 之前）插入：
```js
var _refreshFn = { village:'renderVillageQuestHall()', questboard:'renderQuestBoard()', workspace:'renderWorkspace()', member:opts.memberName?"renderMemberDetail('"+escAttr(opts.memberName)+"')":'', taskhall:"renderDailyView('staff')" };
```

**位置**：第 2755 行，onclick 行尾替换：
```js
// 旧：'+(ctx==='village'?'renderVillageQuestHall()':ctx==='questboard'?'renderQuestBoard()':'renderWorkspace()')+'
// 新：
'+(_refreshFn[ctx]||'')+'
```

### 改动 1.2：索引保护（修复阻塞问题 5.1）

**位置**：第 2733 行

**旧代码**：
```js
var tasks = data.tasks || []; var idx = tasks.indexOf(t);
```

**新代码**：
```js
var tasks = data.tasks || []; var idx = (opts.taskIndex !== undefined) ? opts.taskIndex : tasks.indexOf(t);
```

### 改动 1.3：加 showStatusEdit 开关

**位置**：在操作按钮区（约第 2790 行 `h += '<div style="margin-top:5px;...` 之后），在 locked/isDone 判断之前，插入：

```js
// 当前用于成员档案场景，状态值对齐现有下拉框（待开始/进行中/已完成）
if (opts.showStatusEdit) {
  var statuses = ['待开始','进行中','已完成'];
  h += '<select onchange="data.tasks['+idx+'].status=this.value;localStorage.setItem(\"camp_data\",JSON.stringify(data));updateAll();'+(_refreshFn[ctx]||'')+'" style="font-size:.68rem;padding:1px 4px;border:1px solid var(--border);border-radius:4px;margin-right:4px">';
  statuses.forEach(function(s){ h += '<option value="'+s+'"'+(t.status===s?' selected':'')+'>'+s+'</option>'; });
  h += '</select>';
}
```

注意：onchange 中增加了 `updateAll()` 调用，确保状态下拉修改后统计（NT 余额、成员进度等）同步刷新。

### 改动 1.4：加 showAssignee 开关

**位置**：在 meta 行（约第 2763 行），紧跟在 `'<span>🧙 '+esc(t.poster||'?')+'</span>'` 之后，插入：

```js
if (opts.showAssignee) h += '<span>👤 '+esc(t.assignee||'—')+'</span>';
```

### 改动 1.5：header onclick 增加 stopPropagation（修复阻塞问题 C）

**位置**：第 2755 行

在 `onclick="` 之后立即插入 `event.stopPropagation();`，防止工会大厅场景中卡片展开事件冒泡到外层 `dailyCheckTask`。

**旧代码**（约第 2755 行）：
```js
h += '<div style="display:flex;align-items:center;gap:6px;cursor:pointer" onclick="window._cardExpanded[\''+escAttr(t.name)+'_'+ctx+'\']=!window._cardExpanded[\''+escAttr(t.name)+'_'+ctx+'\'];'+(_refreshFn[ctx]||'')+'">';
```

**新代码**：
```js
h += '<div style="display:flex;align-items:center;gap:6px;cursor:pointer" onclick="event.stopPropagation();window._cardExpanded[\''+escAttr(t.name)+'_'+ctx+'\']=!window._cardExpanded[\''+escAttr(t.name)+'_'+ctx+'\'];'+(_refreshFn[ctx]||'')+'">';
```

注意：`stopPropagation()` 不影响 village/questboard/workspace/member 场景（这些场景卡片外层无额外 onclick），仅在 taskhall 场景中阻止冒泡到外层的 `dailyCheckTask`。

### 验证 Step 1

1. node --check 语法检查通过
2. 村口/主界面任务大厅展开/折叠正常（验证映射表未破坏已有功能）
3. 村口/主界面展开/折叠不触发任何额外副作用（验证 stopPropagation 无副作用）
4. Console 无报错

---

## Step 2：改造砚仁工作台（行 2521-2694）

### 改动 2.1：任务卡片改用 renderQuestCard

**位置**：第 2660-2684 行

**旧代码**（紧凑行式布局，约 25 行）：
```js
// Task cards
if (filtered.length === 0) {
  html += '<div class="card" style="text-align:center;padding:40px;color:var(--muted)">';
  html += '<div style="font-size:2rem;margin-bottom:8px">📋</div>';
  html += '<div>' + (wsTabFilter === 'all' ? '还没有任务。去任务大厅领取吧！' : '没有匹配的任务') + '</div>';
  html += '</div>';
} else {
  html += '<div class="card" style="padding:10px 14px"><div style="display:flex;flex-direction:column;gap:5px">';
  filtered.forEach(function(e) {
    var t = e.task, c = e.claim;
    var st = statusLabel[c.status] || c.status;
    var sc = scopeIcon[e.scope] || '';
    var bgColor = c.status === 'completed' ? '#e8f0e2' : (c.status === 'submitted' ? '#fef9ee' : '#fefdf9');
    var borderColor = c.status === 'completed' ? 'var(--green)' : (c.status === 'submitted' ? '#e8c840' : 'var(--border)');
    html += '<div style="display:flex;align-items:center;gap:10px;background:'+bgColor+';border:1px solid '+borderColor+';border-radius:8px;padding:10px 14px;font-size:.82rem">';
    html += '<span style="font-size:1.1rem;flex-shrink:0">'+sc+'</span>';
    html += '<div style="flex:1;min-width:0">';
    html += '<div style="font-weight:600">'+esc(t.name)+' <span style="font-size:.7rem;color:var(--muted);font-weight:400">NT '+(t.points||0)+' · '+esc(t.deadline||'—')+(t.start ? ' · '+esc(t.start)+'→' : '')+'</span></div>';
    html += '<div style="font-size:.72rem;color:var(--muted)">'+st+'</div>';
    html += '</div>';
    html += '<div style="flex-shrink:0">';
    if (c.status === 'in_progress' || c.status === 'triggered') html += '<button class="btn green" onclick="submitTask('+e.idx+')" style="font-size:.7rem;padding:4px 10px">📤 提交</button>';
    else if (c.status === 'rejected') html += '<button class="btn" onclick="submitTask('+e.idx+')" style="font-size:.7rem;padding:4px 10px">🔄 重交</button>';
    else if (c.status === 'submitted') html += '<span style="font-size:.7rem;color:var(--muted)">⏳ 等待审核</span>';
    else if (c.status === 'completed') html += '<span style="font-size:.7rem;color:var(--green)">✅ 已通过</span>';
    html += '</div></div>';
  });
  html += '</div></div>';
}
```

**新代码**：
```js
// Task cards — unified renderQuestCard
if (filtered.length === 0) {
  html += '<div class="card" style="text-align:center;padding:40px;color:var(--muted)">';
  html += '<div style="font-size:2rem;margin-bottom:8px">📋</div>';
  html += '<div>' + (wsTabFilter === 'all' ? '还没有任务。去任务大厅领取吧！' : '没有匹配的任务') + '</div>';
  html += '</div>';
} else {
  html += '<div class="card" style="padding:10px 14px">';
  filtered.forEach(function(e) {
    html += renderQuestCard(e.task, {context:'workspace', userName:currentUser.name, isMember:true, taskIndex:e.idx});
  });
  html += '</div>';
}
```

### 改动 2.2：过滤体系 — 三标签（共建者专属）+ 六过滤器

**位置**：第 2651-2657 行（现有 filter bar）

**旧代码**（当前实际代码，在 `renderWorkspace` 中约第 2651 行）：
```js
// Filter bar
html += '<div style="display:flex;gap:4px;margin-bottom:12px;flex-wrap:wrap">';
filters.forEach(function(f) {
  var active = wsTabFilter === f.key;
  html += '<button class="btn'+(active?' pri':'')+'" onclick="wsTabFilter=\''+f.key+'\';renderWorkspace()" style="font-size:.75rem;padding:4px 10px">'+f.label+(f.count>0?' <b>'+f.count+'</b>':'')+'</button>';
});
html += '</div>';
```

**新代码**：在现有 filter bar 之前插入三标签层（仅共建者/管理员可见）：
```js
// ═══ 第一层：任务来源标签（仅共建者/管理员可见）═══
if (currentUser.role === 'admin' || currentUser.role === 'builder') {
  html += '<div style="display:flex;gap:4px;margin-bottom:8px;flex-wrap:wrap">';
  var sourceTabs = [
    {key:'all', label:'📋 全部'},
    {key:'work', label:'🏛️ 创世工作'},
    {key:'quest', label:'📋 大厅委托'},
    {key:'personal', label:'✏️ 私人委托'}
  ];
  sourceTabs.forEach(function(f) {
    var active = wsSourceFilter === f.key;
    html += '<button class="btn'+(active?' pri':'')+'" onclick="wsSourceFilter=\''+f.key+'\';renderWorkspace()" style="font-size:.78rem;padding:5px 12px">'+f.label+'</button>';
  });
  html += '</div>';
}
// ═══ 第二层：scope + 状态过滤（现有代码不变）═══
html += '<div style="display:flex;gap:4px;margin-bottom:12px;flex-wrap:wrap">';
filters.forEach(function(f) {
  var active = wsTabFilter === f.key;
  html += '<button class="btn'+(active?' pri':'')+'" onclick="wsTabFilter=\''+f.key+'\';renderWorkspace()" style="font-size:.75rem;padding:4px 10px">'+f.label+(f.count>0?' <b>'+f.count+'</b>':'')+'</button>';
});
html += '</div>';
```

### 改动 2.3：数据过滤 — 在构建时标记任务来源，过滤时先按来源再按状态

**位置 A**（在 `allMyTasks` 构建时增加来源标记）：第 2536-2545 行区域

**旧代码**（第 2543-2544 行）：
```js
if (!myClaim) return;
allMyTasks.push({ task: t, idx: i, claim: myClaim, scope: scope });
```

**新代码**：
```js
if (!myClaim) return;
// 标记任务来源：work=创世分配, quest=大厅领取, personal=私人委托
var source = 'work'; // 默认：assignee 直接分配（无 claimants 记录）
var hasRealClaim = (t.claimants || []).some(function(c) {
  return c.name === name && c.status !== 'dropped';
});
if (hasRealClaim) source = 'quest';
if (scope === 'personal') source = 'personal';
allMyTasks.push({ task: t, idx: i, claim: myClaim, scope: scope, source: source });
```

注意：当 `scope==='personal'` 时 source 优先设为 `'personal'`；当 scope 为 camp 时，根据 claimants 中是否有该用户的真实记录来区分 `'work'`（assignee 分配）和 `'quest'`（大厅主动领取）。

**位置 B**（在现有过滤逻辑之前，约第 2565 行，先按来源过滤）：

在 `var filtered = allMyTasks.filter(...)` 之前插入：
```js
// 先按来源标签过滤
var bySource = allMyTasks;
if (wsSourceFilter && wsSourceFilter !== 'all') {
  bySource = allMyTasks.filter(function(e) {
    return e.source === wsSourceFilter;
  });
}
```

然后将现有过滤逻辑的第 2565 行：
```js
var filtered = allMyTasks.filter(function(e) {
```

改为：
```js
var filtered = bySource.filter(function(e) {
```

### 改动 2.4：全局变量声明

**位置**：第 2695 行（`var flowFilter = 'all';` 之后）

插入新行：
```js
var wsSourceFilter = 'all';
```

### 验证 Step 2

1. 共建者登录 → 工作台 → 看到四标签 `[📋全部] [🏛️创世工作] [📋大厅委托] [✏️私人委托]`
2. 冒险者登录 → 工作台 → 无来源标签，直接看六过滤器
3. 点击来源标签切换 → 任务列表按来源过滤
4. 来源标签 + 六过滤器可以叠加使用（先来源，再状态）
5. 任务卡片可展开看详情
6. node --check 语法通过

---

## Step 3：改造成员档案（行 3923-3986）

### 改动 3.1：任务视图改用 renderQuestCard

**位置**：第 3969-3982 行（`renderMemberDetailFor` 中的任务视图渲染）

**旧代码**（约在 3969 行开始的 `<div class="ms-task-col">` 内部，包含 `tasks.sort(...).map(function(t){ ... })` 的表格行渲染 + 状态下拉 + 材料编辑）：
```js
(view === 'materials' ? renderMaterialSheet(name, memInfo) :
 (tasks.length > 0 ? '<div class="ms-task-col"><h4>📋 任务与材料 (' + tasks.length + '项)</h4>' +
   tasks.sort(function(a,b){return parseDate(a.deadline)-parseDate(b.deadline)}).map(function(t) {
     var mats = t.materials || [];
     var actualMats = (memInfo.actual_materials||{})[t.name] || [];
     var done = t.status==='已完成'||t.status==='已确认';
     return '<div style="border:1px solid var(--border);border-radius:6px;padding:6px 8px;margin-bottom:4px;font-size:.78rem">' +
       '<div style="display:flex;justify-content:space-between;align-items:center">' +
       '<span style="' + (done?'text-decoration:line-through;color:var(--muted)':'') + '">' + esc(t.name) + '</span>' +
       '<select onchange="updateTaskStatus(\'' + escAttr(name) + '\',\'' + escAttr(t.name) + '\',this.value)" style="font-size:.68rem;padding:1px 4px;border:1px solid var(--border);border-radius:4px">' +
       '<option value="待开始"' + (t.status==='待开始'?' selected':'') + '>⬜</option><option value="进行中"' + (t.status==='进行中'?' selected':'') + '>🔄</option><option value="已完成"' + (t.status==='已完成'?' selected':'') + '>✅</option></select></div>' +
       (mats.length > 0 ? '<div style="margin-top:4px;font-size:.7rem;color:var(--muted)">预算材料：' + mats.map(function(m,i){ var a = actualMats[i]||{}; return '<div style="display:flex;gap:4px;align-items:center;margin:2px 0"><span style="flex:1">'+esc(m.item||'')+'</span><span style="color:var(--muted)">预算¥'+(m.rmb||0)+'</span><input class="inline-input" value="'+(a.actual_rmb!=null?a.actual_rmb:'')+'" onchange="updateActualMat(\''+escAttr(name)+'\',\''+escAttr(t.name)+'\','+i+',\'rmb\',this.value)" placeholder="实际¥" style="width:55px;font-size:.68rem"></div>'; }).join('') + '</div>' : '') +
     '</div>';
   }).join('') + '</div>' : '')
```

**新代码**：任务卡片改用 renderQuestCard，材料编辑仅在材料表视图进行：
```js
(view === 'materials' ? renderMaterialSheet(name, memInfo) :
 (tasks.length > 0 ? '<div class="ms-task-col"><h4>📋 任务 (' + tasks.length + '项)</h4>' +
   tasks.sort(function(a,b){return parseDate(a.deadline)-parseDate(b.deadline)}).map(function(t) {
     // staffData/playerData 中 tasks 元素是 data.tasks 的引用，indexOf 返回正确索引
     var idx = data.tasks.indexOf(t);
     return renderQuestCard(t, {context:'member', userName:name, isMember:true, showStatusEdit:true, showAssignee:true, taskIndex:idx, memberName:name});
   }).join('') + '</div>' : '') +
 (tasks.length === 0 ? '<div style="color:var(--muted);text-align:center;padding:20px">' + (type==='player' ? '🎨 冒险者 · 暂无分配任务<br><span style="font-size:.8rem">NT 收入来自活动奖励、拍卖分成等</span>' : '暂无任务') + '</div>' : ''))
```

注意：`memberName` 参数用于展开回调中的 `renderMemberDetail('memberName')` 调用。`userName:name` 传入的是被查看成员的名字（用于判断 myClaim 显示被查看人的领取状态）。

### 改动 3.2：材料编辑分离

**不修改**。材料编辑已在 `renderMaterialSheet(name, memInfo)` 中实现，与任务卡片视图通过 `memberViewMode[name]` 切换分开。任务卡片视图只显示任务信息 + 状态下拉（管理员可见），材料编辑仅在"📦 材料表"视图中进行。

### 验证 Step 3

1. 管理员 → 成员档案 → 点一个人 → 任务卡片可展开/折叠
2. 状态下拉框可切换任务状态
3. 点「📦 材料表」→ 材料编辑界面正常
4. node --check 语法通过
5. 展开/折叠正常（member context 的回调不会触发 workspace 渲染）

---

## Step 4：工会大厅卡片统一（行 3373-3536）

### 改动 4.1：卡片渲染改为 renderQuestCard + 外部装饰保留

**位置**：约第 3494-3535 行（`renderDailyView` 中的任务卡片渲染区）

**旧代码**：`center.innerHTML = activeToday.map((t, i) => { ... })` 包含自定义卡片 HTML（协作者头像、物品展示、切换状态按钮），约 42 行。

**新代码**：卡片主体改用 renderQuestCard，协作者头像和物品展示作为外部装饰保留在卡片外部：
```js
center.innerHTML = activeToday.map(function(t, i) {
  var idx = data.tasks.indexOf(t);
  // ═══ 卡片主体：renderQuestCard ═══
  var cardHTML = renderQuestCard(t, {context:'taskhall', userName:currentUser?currentUser.name:'', isMember:true, showAssignee:true, taskIndex:idx});

  // ═══ 旧 decorate: 协作者头像 — 保留在卡片上方 ═══
  var collabs = [...new Set(tasks.filter(function(ct) {
    return ct !== t && (ct.name.includes(t.name.slice(0,4)) || t.name.includes(ct.name.slice(0,4)));
  }).map(function(ct) { return ct.assignee; }).filter(Boolean))].slice(0, 4);
  var avatarHTML = '';
  if (collabs.length > 0) {
    avatarHTML += '<div style="display:flex;align-items:center;gap:6px;margin-top:6px;font-size:.65rem;color:var(--muted)">'+
      '<span>🤝 协作：</span>'+
      collabs.map(function(c) {
        return '<span style="display:inline-flex;align-items:center;gap:2px;background:#f8f6f0;border-radius:6px;padding:1px 6px">'+
          avatarCircle(c,24,0)+' <span style="font-size:.65rem">'+esc(c)+'</span></span>';
      }).join('')+
    '</div>';
  }

  // ═══ 旧 decorate: 物品/材料 — 保留在卡片下方 ═══
  var items = (t.note || '').split(/[，,、]/).filter(function(s) { return s.trim(); }).slice(0, 5);
  var matHTML = '';
  if (items.length > 0) {
    matHTML += '<div style="display:flex;align-items:center;gap:6px;margin-top:6px;flex-wrap:wrap">'+
      '<span style="font-size:.68rem;color:var(--muted)">🎒 物品：</span>'+
      items.map(function(it) { return '<span style="font-size:.68rem;background:#f0ece0;border-radius:4px;padding:2px 6px">📎 '+esc(it)+'</span>'; }).join('')+
    '</div>';
  }

  // ═══ 外层：dailyCheckTask 点击（stopPropagation 已在 Step 1.5 修复冒泡）═══
  return '<div onclick="dailyCheckTask('+idx+',\''+role+'\')" style="cursor:pointer">'+avatarHTML+cardHTML+matHTML+'</div>';
}).join('');
```

关键：
- **卡片内部变，卡片外部不变**。`renderQuestCard` 只替换卡片本体
- 协作者头像和物品展示仍由 `renderDailyView` 在卡片外部渲染
- `stopPropagation()`（Step 1.5）阻止卡片 header 的展开/折叠事件冒泡到外层 div，避免意外触发 `dailyCheckTask`

### 验证 Step 4

1. 工会大厅 → 日期视图正常
2. 卡片可以展开/折叠（点击 header 不会触发 dailyCheckTask）
3. 点击卡片非 header 区域 → 正常切换状态
4. 协作者头像正常显示
5. 物品/材料展示正常
6. node --check 语法通过

---

## 五、执行后全局验证

全部 Step 完成后：

1. **村口**：登录 → 村口 → 任务大厅 → 卡片展开/折叠 → 领取/提交/放弃
2. **主界面**：任务大厅 tab → 卡片同村口 → 三分区正常
3. **砚仁**：共建者看四来源标签 + 六过滤器 → 标签叠加过滤正常 → 冒险者看六过滤器
4. **成员档案**：点人 → 卡片展开 + 状态下拉（下拉修改后统计同步） → 材料表切换正常
5. **工会大厅**：日期视图 → 卡片 + 头像 + 物品 → 展开/折叠不误触状态切换 → 点击非 header 区域正常切换状态
6. **无回归**：五个场景的原有功能全部正常

---

## 六、本轮修复记录

| 编号 | 问题来源 | 修复内容 | 涉及改动 |
|------|---------|---------|---------|
| A+B | 审查报告执行版 | Step 2.2 旧代码修正为实际代码；Step 2.3 过滤逻辑补全（在 allMyTasks 构建时标记 source，过滤时分两层） | 2.2, 2.3, 2.4 |
| C | 审查报告执行版 | 新增改动 1.5：header onclick 增加 `event.stopPropagation()` | 1.5 |
| D | 审查报告执行版 | 改动 1.3 onchange 增加 `updateAll()` 调用 | 1.3 |
