请执行台词仿写任务。

用户输入格式：`/台词仿写 <角色名> <仿写意图>`

## 执行步骤

1. 解析用户输入中的角色名和仿写意图。

2. 运行以下命令：
```powershell
cd "c:\Users\苏砚仁\thinknote\小说创作\台词工具"
$env:DEEPSEEK_API_KEY = "<你的 DeepSeek API Key，勿硬编码；建议设为系统环境变量>"
& .\.venv\Scripts\python.exe generate.py -c "<角色名>" -i "<仿写意图>"
```

3. 解析脚本输出，将【新台词】以醒目方式展示给用户。

4. 如果脚本报错（如角色名不存在），运行以下命令列出可用角色：
```powershell
cd "c:\Users\苏砚仁\thinknote\小说创作\台词工具"
& .\.venv\Scripts\python.exe -X utf8 -c "import json; lines=[json.loads(l) for l in open('dialogues_final.jsonl',encoding='utf-8') if l.strip()]; from collections import Counter; chars=Counter(l['character'] for l in lines); [print(f'  {c}: {n}条') for c,n in chars.most_common(40)]"
```

5. 不要修改脚本代码。不要重新提取或重建索引（这些是离线任务）。

## 技术信息

- 语料：《大唐双龙传》黄易，约 3.8M 字
- 索引：TF-IDF 字符级 n-gram（1~3），10,000 维
- LLM：DeepSeek V4 Flash，temperature=0.3，非思考模式
- 检索：余弦相似度 Top 40，Jaccard 去重
- 语料路径：`台词工具/dialogues_final.jsonl`（22,620 条，303 角色）
- 索引路径：`台词工具/chroma_db/`
